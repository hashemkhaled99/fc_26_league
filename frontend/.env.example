# Backend service public URL (Northflank backend service URL)
NEXT_PUBLIC_API_URL=

# Socket.io URL (same as backend — API + sockets run on one service)
NEXT_PUBLIC_SOCKET_URL=

# Northflank sets PORT automatically — do not set manually
# PORT=
